import { ComingSoonOverlay } from '../ComingSoonOverlay';

export const VisionView = () => (
  <ComingSoonOverlay
    title="VISION"
    description="Add screen awareness so Jarvis can read what is on your display and guide or automate visual tasks."
  />
);