import { ComingSoonOverlay } from '../ComingSoonOverlay';

export const ThemesView = () => (
  <ComingSoonOverlay
    title="THEMES"
    description="Customise Jarvis with colour schemes, accent colours, fonts, and layout presets to make it truly yours."
  />
);