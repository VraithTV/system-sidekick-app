import { ComingSoonOverlay } from '../ComingSoonOverlay';

export const MeetingsView = () => (
  <ComingSoonOverlay
    title="MEETINGS"
    description="Handle call notes, instant summaries, and action items from voice conversations and recordings."
  />
);