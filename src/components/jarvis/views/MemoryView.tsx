import { ComingSoonOverlay } from '../ComingSoonOverlay';

export const MemoryView = () => (
  <ComingSoonOverlay
    title="MEMORY VAULT"
    description="Give Jarvis a place to store preferences, people, projects, and recurring context for more personal replies."
  />
);